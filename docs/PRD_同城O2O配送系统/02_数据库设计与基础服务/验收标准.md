# 阶段02 验收标准

> 版本: v1.0
> 状态: 基线版
> 所属阶段: 02_数据库设计与基础服务

---

## 一、验收总则

本阶段验收目标：确认数据库表结构完整正确、所有基础服务接口可调通、认证鉴权流程闭环、中间件功能正常。

通过条件为：全部必检项通过。

---

## 二、数据库表结构完整性检查

### 2.1 表创建确认 [必检]

| 序号 | 表名 | 检查方法 | 结果 |
|------|------|----------|------|
| DB-01 | users | `DESC users` 字段数量和类型正确 | [ ] |
| DB-02 | user_addresses | `DESC user_addresses` | [ ] |
| DB-03 | merchants | `DESC merchants` | [ ] |
| DB-04 | stores | `DESC stores` | [ ] |
| DB-05 | riders | `DESC riders` | [ ] |
| DB-06 | product_categories | `DESC product_categories` | [ ] |
| DB-07 | products | `DESC products` | [ ] |
| DB-08 | skus | `DESC skus` | [ ] |
| DB-09 | orders | `DESC orders` | [ ] |
| DB-10 | order_items | `DESC order_items` | [ ] |
| DB-11 | errand_orders | `DESC errand_orders` | [ ] |
| DB-12 | payments | `DESC payments` | [ ] |
| DB-13 | aftersales | `DESC aftersales` | [ ] |
| DB-14 | settlements | `DESC settlements` | [ ] |
| DB-15 | withdrawals | `DESC withdrawals` | [ ] |
| DB-16 | rider_tracks | `DESC rider_tracks` | [ ] |
| DB-17 | messages | `DESC messages` | [ ] |
| DB-18 | audit_logs | `DESC audit_logs` | [ ] |
| DB-19 | configs | `DESC configs` | [ ] |
| DB-20 | areas | `DESC areas` | [ ] |
| DB-21 | pricing_rules | `DESC pricing_rules` | [ ] |
| DB-22 | coupons | `DESC coupons` | [ ] |
| DB-23 | coupon_records | `DESC coupon_records` | [ ] |
| DB-24 | admins | `DESC admins` | [ ] |
| DB-25 | roles | `DESC roles` | [ ] |
| DB-26 | role_permissions | `DESC role_permissions` | [ ] |
| DB-27 | banners | `DESC banners` | [ ] |
| DB-28 | order_logs | `DESC order_logs` | [ ] |
| DB-29 | complaints | `DESC complaints` | [ ] |
| DB-30 | blacklist | `DESC blacklist` | [ ] |

### 2.2 索引和约束检查 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| DB-31 | 所有唯一索引已创建 | `SHOW INDEX FROM {table}` 确认 UNIQUE 索引存在 | [ ] |
| DB-32 | 所有查询索引已创建 | `SHOW INDEX FROM {table}` 确认普通索引存在 | [ ] |
| DB-33 | 字符集正确 | 所有表字符集为 utf8mb4 | [ ] |
| DB-34 | 引擎正确 | 所有表引擎为 InnoDB | [ ] |
| DB-35 | 金额字段类型正确 | 金额字段为 DECIMAL(10,2) | [ ] |
| DB-36 | 默认值正确 | created_at/updated_at 有默认值 | [ ] |

### 2.3 种子数据检查 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| DB-37 | 超级管理员角色已创建 | `SELECT * FROM roles WHERE code='SUPER_ADMIN'` 有数据 | [ ] |
| DB-38 | 超级管理员账号已创建 | `SELECT * FROM admins WHERE username='admin'` 有数据 | [ ] |
| DB-39 | 管理员密码可登录 | 使用默认密码可成功登录 | [ ] |

---

## 三、基础服务接口验证

### 3.1 网关基础功能 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| GW-01 | 健康检查可用 | `GET /api/v1/common/health` 返回 200 | [ ] |
| GW-02 | 请求日志记录 | 发送请求后，日志文件中有对应记录 | [ ] |
| GW-03 | 错误处理 | 访问不存在路由返回 `{ code: 10002, message: "..." }` | [ ] |
| GW-04 | CORS 生效 | 跨域请求响应头包含 Access-Control-Allow-Origin | [ ] |
| GW-05 | 限流生效 | 短时间内大量请求返回 429 | [ ] |
| GW-06 | 请求体解析 | POST JSON 请求体可正确解析 | [ ] |

### 3.2 认证授权 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| AU-01 | Token签发正确 | JWT解析后包含 userId/role/iat/exp | [ ] |
| AU-02 | Token过期拒绝 | 使用过期Token请求返回 11001 | [ ] |
| AU-03 | 无Token拒绝 | 不携带Token请求受保护接口返回 11001 | [ ] |
| AU-04 | Token刷新 | POST /api/v1/auth/refresh-token 返回新Token对 | [ ] |
| AU-05 | 登出生效 | 登出后使用旧Token返回 11002 | [ ] |
| AU-06 | 权限中间件 | 无权限角色访问返回 11003 (403) | [ ] |

### 3.3 用户登录流程 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| UL-01 | 微信登录（新用户） | 使用微信code登录，返回Token且isNewUser=true | [ ] |
| UL-02 | 微信登录（老用户） | 同一openid再次登录，isNewUser=false | [ ] |
| UL-03 | 手机号绑定 | 绑定手机号成功 | [ ] |
| UL-04 | 重复手机号绑定 | 已绑定手机号绑定另一账号返回 12001 | [ ] |
| UL-05 | 更新个人信息 | PUT profile 更新昵称和头像成功 | [ ] |

### 3.4 商家登录流程 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| ML-01 | 商家注册 | 手机号+验证码注册成功，状态为PENDING | [ ] |
| ML-02 | 重复注册 | 同一手机号再次注册返回 13004 | [ ] |
| ML-03 | 验证码登录 | 手机号+验证码登录成功 | [ ] |
| ML-04 | 密码登录 | 手机号+密码登录成功 | [ ] |
| ML-05 | 错误密码 | 密码错误返回 13002 | [ ] |
| ML-06 | 冻结商家 | 状态为FROZEN的商家登录返回 13003 | [ ] |

### 3.5 骑手登录流程 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| RL-01 | 骑手注册 | 手机号+验证码注册成功，状态PENDING，在线OFFLINE | [ ] |
| RL-02 | 重复注册 | 同一手机号再次注册返回 14004 | [ ] |
| RL-03 | 验证码登录 | 登录成功 | [ ] |
| RL-04 | 冻结骑手 | 状态为FROZEN的骑手登录返回 14003 | [ ] |

### 3.6 管理员登录流程 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| AL-01 | 管理员登录 | admin账号+密码登录成功 | [ ] |
| AL-02 | 获取管理员信息 | 登录后GET info返回角色名和权限列表 | [ ] |
| AL-03 | 错误密码 | 密码错误返回明确提示 | [ ] |
| AL-04 | 不存在用户名 | 用户名不存在返回明确提示 | [ ] |
| AL-05 | 登录审计 | 登录后audit_logs表有记录 | [ ] |

### 3.7 短信验证码 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| SM-01 | 发送验证码 | POST send-code 返回成功 | [ ] |
| SM-02 | 频率限制 | 60秒内重发被拒绝 | [ ] |
| SM-03 | 格式校验 | 错误手机号格式返回 10001 | [ ] |
| SM-04 | 验证码过期 | 5分钟后验证码失效 | [ ] |
| SM-05 | 验证码使用后删除 | 验证成功后同一验证码不能再用 | [ ] |
| SM-06 | 开发环境模拟 | 开发环境可使用固定验证码 | [ ] |

### 3.8 文件上传 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| FU-01 | 上传图片 | POST upload/image 返回URL | [ ] |
| FU-02 | URL可访问 | 返回的URL在浏览器中可查看图片 | [ ] |
| FU-03 | 类型限制 | 上传非图片文件被拒绝 | [ ] |
| FU-04 | 大小限制 | 超过10MB文件上传被拒绝 | [ ] |
| FU-05 | 多图上传 | POST upload/images 正确返回所有URL | [ ] |

### 3.9 通用中间件 [必检]

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| MW-01 | 参数校验 | 缺少必填参数返回 10001 和详细字段信息 | [ ] |
| MW-02 | 分页参数 | page/pageSize 参数正确解析 | [ ] |
| MW-03 | 分页限制 | pageSize > 100 时自动限制为 100 | [ ] |
| MW-04 | 响应格式 | 所有响应遵循 { code, message, data } 格式 | [ ] |

---

## 四、Redis缓存功能验证

| 序号 | 检查项 | 验证方法 | 结果 |
|------|--------|----------|------|
| RC-01 | Token存储 | 登录后Redis中有Token记录 | [ ] |
| RC-02 | Token过期 | 到期后Redis中Token自动消失 | [ ] |
| RC-03 | 黑名单 | 登出后Token加入blacklist | [ ] |
| RC-04 | 验证码存取 | Redis中可查到验证码记录 | [ ] |
| RC-05 | 频率限制 | Redis中有频率限制记录 | [ ] |

---

## 五、闭环流程验证

### 5.1 用户登录闭环 [必检]

完整操作步骤：
1. 调用 `POST /api/v1/auth/user/wx-login` 获取Token → 成功
2. 用Token调用 `POST /api/v1/common/sms/send-code` 发送验证码 → 成功
3. 调用 `POST /api/v1/auth/user/bind-phone` 绑定手机号 → 成功
4. 调用 `PUT /api/v1/auth/user/profile` 更新昵称 → 成功
5. 调用 `POST /api/v1/auth/logout` 登出 → 成功
6. 用旧Token调用任意接口 → 返回11002
7. 用RefreshToken调用 `POST /api/v1/auth/refresh-token` → 失败(已登出)

结果：[ ] 全部通过

### 5.2 商家登录闭环 [必检]

完整操作步骤：
1. 发送验证码 → 成功
2. 注册商家 → 成功，状态PENDING
3. 登出 → 成功
4. 验证码登录 → 成功
5. 密码登录 → 成功
6. 获取个人信息 → 状态为PENDING

结果：[ ] 全部通过

### 5.3 权限隔离验证 [必检]

| 检查项 | 验证方法 | 结果 |
|--------|----------|------|
| 用户Token不能调商家接口 | 用USER Token调商家专属接口返回403 | [ ] |
| 商家Token不能调骑手接口 | 用MERCHANT Token调骑手专属接口返回403 | [ ] |
| 骑手Token不能调管理接口 | 用RIDER Token调管理后台接口返回403 | [ ] |
| 管理员各角色权限隔离 | 非超管角色不能调超管专属接口 | [ ] |

---

## 六、验收结论

| 类别 | 通过数/总数 | 结论 |
|------|-----------|------|
| 数据库表结构 | __/39 | [ ] 通过 / [ ] 不通过 |
| 网关基础 | __/6 | [ ] 通过 / [ ] 不通过 |
| 认证授权 | __/6 | [ ] 通过 / [ ] 不通过 |
| 用户登录 | __/5 | [ ] 通过 / [ ] 不通过 |
| 商家登录 | __/6 | [ ] 通过 / [ ] 不通过 |
| 骑手登录 | __/4 | [ ] 通过 / [ ] 不通过 |
| 管理员登录 | __/5 | [ ] 通过 / [ ] 不通过 |
| 短信验证码 | __/6 | [ ] 通过 / [ ] 不通过 |
| 文件上传 | __/5 | [ ] 通过 / [ ] 不通过 |
| 通用中间件 | __/4 | [ ] 通过 / [ ] 不通过 |
| Redis缓存 | __/5 | [ ] 通过 / [ ] 不通过 |
| 闭环流程 | __/3组 | [ ] 通过 / [ ] 不通过 |

**总体结论：** [ ] 通过，可进入阶段03 / [ ] 不通过，需补充以下项：
