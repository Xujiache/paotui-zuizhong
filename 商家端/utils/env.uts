// uni-app x 环境常量（uni-app x 对 .env.* 支持有限，这里按条件编译切换）
// 开发时默认指向本机后端；发布前按环境修改 PROD_BASE_URL

const DEV_BASE_URL = 'http://localhost:3000'
const PROD_BASE_URL = 'https://api.example.com'

// #ifdef APP-ANDROID || APP-IOS || APP-HARMONY
// 原生 APP 调试时指向开发主机（若用模拟器，改为主机局域网 IP）
export const BASE_URL = DEV_BASE_URL
// #endif

// #ifdef H5
export const BASE_URL = process.env.NODE_ENV === 'production' ? PROD_BASE_URL : DEV_BASE_URL
// #endif

// #ifdef MP-WEIXIN
// 小程序：请在微信开发者工具的"项目配置"中关闭"不校验合法域名"或加入白名单
export const BASE_URL = DEV_BASE_URL
// #endif

export const CLIENT_TYPE = 'merchant-app'
